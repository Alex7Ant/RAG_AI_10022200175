# Pip-only runtime dependencies. Heavy numeric packages are installed via conda (see scripts/env_setup_ps1).
streamlit==1.44.1
pypdf==5.4.0
python-dotenv==1.1.0
openai==1.74.0
requests==2.32.3
fastapi==0.95.2
uvicorn[standard]==0.22.0

# Heavy numeric / ML packages (required for local behaviour and for Streamlit Cloud deployments)
# Heavy ML packages removed for Streamlit Cloud (can be installed via Conda or
# used in the Container/Docker deployment). Options:
# - Deploy to Render using the provided Conda-based Dockerfile (recommended),
# - Or add these to a Conda environment locally / on your own server.
